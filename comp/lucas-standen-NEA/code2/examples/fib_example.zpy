(defun fib int n:int)
	(if (< n 2))
		(return n)
	(endif)

	(return (+ (fib (- n 1)) (fib (- n 2))))
(endfun)

(defun main int)
	(printstr "give me a number (bellow 30 if you want it to be quick): ")
	(let n:int (readint))
	(printint (fib n))
	(printchar '\n')
(endfun)
